#include<stdio.h>
int factorial(int);
int main()
{
	int a,b;
	printf("Enter a number:");
	scanf("%d",&a);
	b=factorial(a);
	printf("Factorial of the number is %d",b);
}
int factorial(int a)
{
	int i,b=1;
	if (n==0 || n==0)
		return 1;
	else
		return n*factorial(n-1);
}
